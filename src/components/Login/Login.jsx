import { AuthCard } from '../AuthCard';

export const Login = () => {
  return (
    <div className='bg-primary h-screen flex justify-center items-center'>
      <AuthCard type='login' />
    </div>
  );
};
